# benchmarks

Generator i mutation skripte za evaluaciju (plan.md, sekcija 4.4 i Faza 3):
generisanje sintetičkih `.ks` programa po topologiji (`leaf-only`, `chain`,
`hub-and-spoke`, `random-dag`), mutacija postojećih programa, i harness koji
poredi vreme baseline (pune) rekompilacije sa inkrementalnom.

Prazno za sada — puni se u Fazi 3.
